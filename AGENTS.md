# 项目级开发规则

## 当前阶段

- 本项目目前处于测试阶段。
- 默认直接实现需求的最终目标形态，不考虑向后兼容或兼容过渡。
- 无需为旧接口、旧数据格式或旧行为保留兼容层，也无需设计双写、双轨、灰度或迁移过渡方案。
- 当前数据均为测试数据；版本迭代、数据结构调整或模型变更时，默认允许直接清空并重建，不要求编写迁移脚本或保留旧数据。
- 验收以目标功能完成且必要验证通过为准；若具体任务明确要求兼容，则以该任务要求为准。

## 项目规模与开发取向

- 这是一个小规模系统，目前仅有少数用户使用。
- 以快速开发、快速验证和完成当前业务目标为优先。
- 方案应保持简单直接，优先采用现有技术栈和最小可行实现。
- 避免过度抽象、复杂架构、过早优化，以及为尚未出现的规模或场景预留不必要的扩展机制。
- 只有在具体需求或实际风险明确要求时，才引入额外复杂度。

## “请修复”的处理约定

- 当用户说“请修复”或表达同义意图时，表示授权核实并处理当前对话中已经提到的问题，不得未经验证就直接假定问题存在。
- 修复前应逐项核对相关代码、运行行为和已有证据，确认问题在当前工作区中是否仍然存在。
- 对确认存在的问题，直接完成必要的最小修复并执行与风险相匹配的验证；对不存在、已被修复或无法复现的问题，不做无依据的修改，并说明判断原因和核实证据。
- 如果提到的多个问题只有部分存在，应分别说明核实结果，只修复确认存在的部分。

## “请分析当前修改”的处理约定

- 当用户说“请分析当前代码”“请分析当前修改的代码”“请分析修改”或表达同义意图时，默认分析当前工作区和暂存区相对于 `HEAD` 的全部改动。
- 工作区内容代表最新修改；同一文件或同一处代码同时存在暂存与未暂存改动时，应以包含两者后的当前工作区最终内容为主要审查对象，同时核对暂存区，避免遗漏已暂存但工作区中不明显的改动。
- 分析应基于实际差异和必要验证，判断改动是否合理，并检查潜在风险、功能问题、回归、测试缺口以及值得优化或补充完善之处。
- 不得为了给出审查意见而编造问题；如果没有发现实质问题，应直接说明当前修改已较为完善，并如实报告已执行的验证和仍未覆盖的范围。

## Git 提交与推送

- 当用户说“提交”或表达同义意图时，直接执行 Git 提交并推送到当前测试分支，无需额外确认。
- 提交前检查工作区状态、暂存文件列表和暂存差异，仅纳入本次任务明确范围内的改动；用户已有或无关改动必须保留，不得混入提交。
- commit message 使用中文，并简要总结本次实际提交的代码改动，格式为：`feat: <本次修改代码的简明中文总结>`。
- 完成后说明提交、推送结果，并总结本次提交的代码内容。
