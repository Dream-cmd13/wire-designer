# 线束设计器当前系统审计与前端 MVP 完善计划

> 审计日期：2026-07-03
> 审计对象：`wire-harness-designer` 当前工作区
> 结论适用范围：当前 `schemaVersion: 3` 前端单体
> 审计方法：源码与文档交叉审查、ESLint、单元测试、生产构建、`http://localhost:5173/` 实际页面走查
> 本文是当前决策基线；2026-06-29 的旧系统分析仅保留历史参考价值。

---

## 0. 指定条目实施结果（2026-07-03）

本轮仅处理用户指定的 `2.3、3.2、3.3、4.2、4.5、4.6、4.7、4.11、4.12`，其余风险保持原优先级，不在本轮扩展。

| 条目 | 实施结果 |
|---|---|
| 2.3 | 新增线材改为本地 draft，确认后才写 Store；低于 1280px 自动收起右栏并隐藏等距预览 |
| 3.2 | 移除无项目归属的 `harness-config` 双副本；项目文档统一走异步 `ProjectRepository` |
| 3.3 | 增加完整深层运行时结构校验，损坏数据保留原始恢复副本并暂停自动保存 |
| 4.2 | 增加完整设计 JSON 导入/导出和轮换恢复点；导入/恢复均创建新项目，不覆盖现有项目 |
| 4.5 | 新增线材弹窗取消不再改变文档、`updatedAt`、保存状态或撤销历史 |
| 4.6 | 全局撤销/重做跳过 input、textarea、select 和 contenteditable |
| 4.7 | 因状态流转语义尚未确认，按兜底方案移除虚假状态统计，只保留项目总数 |
| 4.11 | 画布删除后显示可撤销 Toast；删除连接器前提示断开的接线/短接数量；换型裁剪显示醒目警告 |
| 4.12 | 设计器、预览、向导、选型器和弹窗按需加载；主入口不再出现 500kB 大包警告 |

---

## 1. 执行结论

### 1.1 是否可以先不考虑后端

**可以，但只能把当前目标定义为“本地单机交互 MVP”。**

现阶段应优先验证：

- 线束设计流程是否自然；
- 连接器、线材、PIN、保护套等领域模型是否正确；
- 画布操作、属性编辑、撤销重做是否好用；
- BOM 和价格估算的展示形式是否符合用户预期；
- 模板、选型和校验规则是否覆盖核心场景。

以下目标不能由当前纯前端方案可靠承担：

- 正式账号、权限和用户数据隔离；
- 跨设备保存、云端备份与多人共享；
- 客户正式生产数据的可靠保存；
- 正式报价、订单、支付或审批；
- 可审计的版本、变更记录和报价快照；
- 多人实时协作。

因此，“先做前端”是合理的产品顺序，但不是永久架构。后端应在**外部客户试用或数据开始具有业务价值之前**接入，而不是等到正式上线前最后补。

### 1.2 当前系统处于什么阶段

当前系统已经不是空壳，也不只是静态原型。它具备：

- 项目列表和四步创建向导；
- 空白项目与模板项目；
- React Flow 画布；
- 连接器、线材、保护套、外模；
- 单端/双端接线、PIN 编辑、同侧短接；
- 标签与号码管；
- 属性面板、右键菜单、撤销/重做；
- 自动保存状态；
- 实时设计校验；
- BOM、估算报价和 SVG 等距预览；
- 51 个领域层与持久化单元测试。

更准确的定位是：

> **功能较丰富的本地前端 Alpha，已能用于内部演示和交互验证，但尚未达到外部客户可放心使用的 MVP。**

### 1.3 当前最高优先级

不建议立即搭建 NestJS，也不建议先做视觉重构。优先顺序应是：

1. 补齐设计文件备份、恢复和运行时数据校验；
2. 修正会污染历史/保存状态的交互事务；
3. 明确本地模式边界，移除“伪安全感”；
4. 补齐生产所需 BOM/目录规则，或明确标注为概念数据；
5. 增加关键流程 E2E；
6. 优化 1024–1280 宽度下的画布可用空间；
7. 完成上述前端闭环后，再接 Supabase Auth + Postgres。

---

## 2. 本次实际核验结果

### 2.1 工程质量基线

| 检查项 | 2026-07-03 结果 | 判断 |
|---|---:|---|
| `npm.cmd run lint` | 通过 | 基础静态质量可接受 |
| `npm.cmd test` | 7 个测试文件、51 个测试全部通过 | 领域命令、结构校验、导入导出和本地仓库已有基础保护 |
| `npm.cmd run build` | 通过 | 可生成生产构建 |
| 主入口 JS | 241.62 kB，gzip 75.90 kB | 已拆分设计器与非首屏弹窗，无 500 kB 提示 |
| 浏览器控制台 | 主流程未发现 warning/error | 基础运行稳定 |
| E2E/组件测试 | 未配置 | 主交互回归仍依赖手工走查 |

### 2.2 浏览器主流程

在 `http://localhost:5173/` 实际完成了：

1. 本地注册；
2. 创建空白项目；
3. 进入空白画布；
4. 通过画布右键添加连接器；
5. 通过画布右键打开线材配置；
6. 取消新增线材；
7. 再次新增 500mm 电子线；
8. 核对 BOM 与估算价格联动；
9. 核对 2 秒自动保存；
10. 检查 1280×720 和 1024×768 布局。

已确认旧文档中的以下结论已失效：

- “空白项目无法继续”——已修复；
- “没有画布右键菜单”——已实现；
- “没有撤销/重做”——已实现；
- “没有自动化测试”——已有 51 个单元测试；
- “没有保存状态”——已显示已保存/未保存/保存中/失败。

### 2.3 浏览器走查问题处理结果

#### A. 新增线材取消已改为无副作用事务

新增线材现在先保存在组件本地 draft 中。只有点击“确定”后才执行一次
`addMaterial`；取消或按 Escape 只关闭弹窗，不修改文档、时间戳、保存状态和撤销历史。

#### B. 1024×768 已提供明确的窄屏降级

- 低于 1280px 首次进入设计器时默认收起右侧 BOM/报价栏；
- 低于 1280px 隐藏悬浮等距预览，避免遮住画布；
- 顶部显示“窄屏模式”说明，右侧栏仍可按需手动展开；
- 左侧配置栏和画布主操作区保持可用。

---

## 3. 当前架构判断

### 3.1 技术栈无需重写

| 层级 | 当前方案 | 结论 |
|---|---|---|
| UI | React 19 + TypeScript | 保留 |
| 构建 | Vite 8 | 保留 |
| 画布 | `@xyflow/react` | 与产品形态匹配，保留 |
| 状态 | Zustand | 当前规模够用 |
| 持久化 | `localStorage` | 只适合本地 MVP |
| 校验 | 自定义领域校验 | 保留，但需增加运行时结构校验 |
| 测试 | Vitest | 保留，并补组件/E2E |
| 后续云端 | Supabase Auth + Postgres | 比先建自有 NestJS 更符合当前阶段 |

当前主要问题不是技术选型，而是：

- 产品边界未完全说清；
- 浏览器存储与项目仓库耦合；
- 运行时数据结构校验不够深；
- 核心交互还缺少端到端保护；
- BOM、目录、报价的业务完整度不足。

### 3.2 项目持久化已收敛为单一文档源

当前职责已经明确：

- `harness-project-config-{projectId}`：唯一的 `HarnessConfig` 持久化副本；
- `harness-projects`：只保存项目列表和名称等元数据；
- `useHarnessStore`：只保存当前会话的内存编辑状态，不再写无项目归属的草稿；
- `ProjectRepository`：提供异步 `list/load/save/remove` 和本地离开前尽力刷新接口。

本地实现仍使用 `localStorage`，但组件和 Store 不再依赖具体存储细节。后续接入
Supabase 时可替换仓库实现，不需要重写编辑器交互。

### 3.3 已增加完整运行时结构校验

`parseHarnessConfig` 现在深层校验：

- 顶层版本、标识、时间、数量、交期和全部集合；
- Connector、PIN 标签、位置和 Jumper；
- Material、两类 wire spec、端部处理、Circuit、路由偏移；
- 标签、号码管、保护套、外模及其嵌套字段。

结构不合法时拒绝作为项目加载，同时保存原始 JSON 到恢复 Key、暂停自动保存并提供下载入口。
结构合法但存在 PIN 悬空、范围冲突等业务问题时仍允许导入修复，并在摘要和校验面板中提示。

---

## 4. 风险清单

优先级定义：

- **P0**：外部试用或正式数据进入前必须处理；
- **P1**：前端 MVP 应处理，否则显著影响可信度或可维护性；
- **P2**：可以在 MVP 验证后优化。

### 4.1 P0：本地“登录”不是认证

证据：

- 密码明文存入 `localStorage`；
- 用户列表和当前用户同样保存在浏览器；
- “切换用户”不要求再次输入密码；
- 用户隔离只是前端数组过滤；
- 浏览器内任何脚本都可读取这些数据。

当前界面已经提示“本地演示登录”，这是正确的，但仍容易让测试用户形成账号系统已完成的错觉。

前端 MVP 推荐二选一：

1. **更推荐：改为“本地工作区/本地身份”，完全去掉密码和登录措辞**；
2. 保留现状，但只用于内部演示，绝不让用户复用真实密码。

真正外部试用时接入 Supabase Auth，并配置用户级 RLS。

### 4.2 P0：已补完整设计文件备份与恢复入口

项目卡片现可导出包含完整 `HarnessConfig`、项目摘要和校验计数的设计 JSON。
项目列表提供导入入口，流程为：

```text
选择文件 → 深层结构校验 → 展示对象/错误摘要 → 确认后创建新项目
```

无论成功或失败都不会覆盖现有项目。项目结构损坏时还会保留原始恢复副本并允许下载。
正常保存会每 5 分钟最多生成一个自动恢复点并轮换保留最近 3 个；项目卡可把最新恢复点另存为新项目。
项目页明确显示“仅保存在此浏览器”和定期导出提醒。

### 4.3 P0：BOM 仍不是生产 BOM

当前 BOM 主要包含：

- 连接器壳体；
- 线材；
- 保护套。

通常仍缺少或未被建模：

- 端子/压接件；
- 密封塞、楔锁、后盖等连接器附件；
- 标签和号码管 BOM；
- 屏蔽层处理、热缩件、扎带等工艺附件；
- 损耗、备品、包装；
- 制造工序与检验要求。

因此可以称为“物料估算”或“概念 BOM”，不能直接称为可下发生产的 BOM，除非业务方确认当前产品范围确实只需要这些物料。

### 4.4 P0：报价不能用于正式交易

当前代码已经诚实标注“估算价格，非正式报价”，这一点应保留。

现有估算仍存在：

- 货币固定显示 `$`，未声明 USD/CNY；
- 不含税费、运费、最小起订量；
- 连接器加工费按连接器数量，而非实际压接端子/PIN/工序；
- 价格规则没有版本号和生效日期；
- 前端常量可被任意修改；
- BOM 总计与报价总计的口径不同。

前端 MVP 可以继续展示估算，但正式报价必须由可信服务端根据版本化规则生成快照。

### 4.5 P1：新增弹窗已改为原子事务

新增线材使用组件本地 draft，确认后才一次性写入 Store。取消不会产生临时对象、
`updatedAt` 变化、保存状态变化或撤销记录。保护套和外模继续保持确认后创建。

### 4.6 P1：输入控件保留原生撤销行为

全局 `Ctrl/Cmd+Z`、`Ctrl/Cmd+Shift+Z` 和 `Ctrl/Cmd+Y` 处理前会检查事件目标。
焦点位于 `input`、`textarea`、`select` 或 `contenteditable` 及其子元素时，不拦截按键。

### 4.7 P1：项目状态展示已收敛

由于“进行中、完成、归档”的业务触发条件和回退规则尚未确认，本轮没有虚构状态机。
项目列表移除了四种不可操作的状态统计和卡片状态徽标，只保留真实可解释的“项目总数”。

### 4.8 P1：目录数据和材料定义仍需业务确认

当前连接器目录明确是演示数据，不能直接指导生产。还需确认：

- 制造商料号是否唯一且准确；
- Housing、Terminal、Seal 是否拆分；
- PIN 编号、视图方向、性别和配对关系；
- 电流、电压、线径、温度和防护等级；
- `PVR` 是否为业务认可材料，还是 `PUR`/其他材料的笔误；
- UL 号与具体线材规格的约束关系。

建议将目录项增加 `catalogVersion`、`source`、`verifiedAt` 和 `productionApproved`。

### 4.9 P1：测试覆盖集中在纯函数，缺关键用户旅程

当前 51 个测试主要覆盖：

- 接线命令；
- 短接；
- 换型裁剪；
- BOM 分组；
- ID 校验；
- 配置深层结构校验；
- 设计文件导入/导出 round-trip；
- 本地项目仓库和损坏数据备份。

尚未覆盖：

- 注册/本地工作区；
- 项目创建、保存、重开、删除；
- 线材弹窗确认/取消；
- 撤销/重做与输入框冲突；
- 画布点击接线；
- 右键菜单；
- 导入/导出的实际 UI 旅程；
- localStorage 容量错误的 UI 反馈；
- 1024/1280 布局。

前端 MVP 至少应增加 5 条 E2E：

1. 空白项目从 0 到建立一条完整接线；
2. 模板项目编辑、自动保存、刷新后恢复；
3. 新增弹窗取消不改变文档和历史；
4. 连接器换型导致 PIN 越界时给出可理解反馈；
5. 设计 JSON 导出后可在新工作区导入并保持等价。

### 4.10 P1：校验结果目前没有形成发布门禁

实时校验已经存在，但当前错误只作为提示展示。估算和 BOM 导出仍可继续。

建议区分：

- 本地保存：任何时候允许；
- 设计 JSON 导出：允许，但附带错误摘要；
- BOM/估算导出：存在结构错误时禁止；
- 正式询价/生产提交：存在任何 Error 时禁止。

### 4.11 P1：破坏性操作已增加结果反馈

- 删除连接器、线材、保护套或外模后显示 6 秒 Toast，并提供“撤销”；
- 删除连接器前计算并提示将断开的接线数量和删除的短接数量；
- 如果删除后继续编辑，Toast 撤销会禁用并提示使用全局撤销，避免覆盖后续修改；
- 连接器换型裁剪越界 PIN 后在属性面板显示醒目的黄色结果警告。

### 4.12 P2：首屏体积已拆分，结构性拆分继续渐进

设计器画布、等距预览、项目向导、认证弹窗、物料选择器以及画布配置弹窗均改为按需加载。
生产构建的主入口从约 536 kB 降至 241.62 kB，gzip 从约 161 kB 降至 75.90 kB，
不再触发 500 kB 大包警告。

`HarnessCanvas.tsx` 仍较大，但已经作为独立路由级 chunk，不阻塞项目列表首屏。
后续如继续维护画布，可在不改变行为的前提下再拆分 projection、connection 和 dialog orchestration；
这属于可维护性演进，不再是当前首屏性能风险。

### 4.13 P2：产品命名和文档一致性

- 顶部显示 `v2.0`，数据模型是 `schemaVersion: 3`，两者含义不同但容易混淆；
- README 的设计导入/导出说明已与实现同步；
- “导出 Excel”实际生成 TSV，不是 XLSX；
- 旧系统分析包含大量已经修复的结论。

这些不会导致运行错误，但会直接影响评审和排期判断。

---

## 5. 前端 MVP 的建议边界

### 5.1 建议纳入

- 本地项目创建、重命名、删除；
- 空白项目和 1–2 个高质量模板；
- 连接器、线材、保护套的完整编辑闭环；
- 单端/双端、多 PIN、短接；
- 撤销/重做；
- 自动保存和恢复；
- 完整设计 JSON 导入/导出；
- 实时校验与错误定位；
- 概念 BOM、估算报价；
- 桌面端 `≥1024px`（1024–1279px 使用窄屏降级）；
- 关键流程 E2E；
- 清晰的“本地模式、非正式报价、演示目录”标记。

### 5.2 建议暂不纳入

- NestJS、自建认证、微服务；
- 多人实时协作；
- 支付和订单；
- 正式报价；
- 真实 3D/CAD；
- 移动端编辑器；
- 复杂审批流；
- 旧 schema/旧项目迁移；
- 高并发和分布式基础设施。

### 5.3 前端 MVP 完成定义

只有同时满足以下条件，才建议邀请外部用户做“体验测试”：

- 新用户无需说明书即可从空白项目完成一条接线；
- 所有新增弹窗取消后文档完全不变；
- 刷新和重新打开项目后数据一致；
- 可导出完整设计文件并成功导入；
- 损坏文件不会使整个应用白屏；
- 关键错误能定位到具体对象；
- 1024px 下有可用降级方案，或明确阻止并说明；
- 5 条核心 E2E 稳定通过；
- BOM/报价/登录的非生产属性在 UI 中明确可见；
- 不要求用户输入或复用真实密码。

---

## 6. 建议实施顺序

### Phase F0：数据可信度

1. [已完成] 增加深层运行时 schema；
2. [已完成] 增加设计 JSON 导入/导出；
3. [已完成] 增加加载失败备份和恢复入口；
4. [已完成] 抽象异步 `ProjectRepository`；
5. [已完成] 合并或明确草稿与项目配置的职责。

验收：项目可备份、可恢复、损坏数据不白屏。

### Phase F1：交互可信度

1. [已完成] 修复新增线材取消副作用；
2. [已完成] 修复输入框内撤销快捷键；
3. [已完成] 删除/换型增加结果反馈；
4. 校验错误与导出门禁联动；
5. [已完成] 删除无效项目状态统计，仅保留项目总数。

验收：取消就是取消，撤销可预测，危险操作有反馈。

### Phase F2：业务可信度

1. 与业务方确认生产 BOM 最小字段；
2. 确认连接器、端子、密封件、标签、号码管范围；
3. 确认币种、估价口径和折扣规则；
4. 给目录和价格增加版本；
5. 明确 `PVR` 等材料定义。

验收：产品不会把演示数据包装成生产数据。

### Phase F3：测试与布局

1. 增加关键 E2E；
2. 增加项目仓库、历史、价格测试；
3. 低于 1280px 自动收起侧栏或预览；
4. 拆分非首屏弹窗代码；
5. 将 `HarnessCanvas` 按职责拆分。

验收：主流程可自动回归，常见桌面尺寸可用。

### Phase B1：轻量云端

当出现以下任一条件时开始：

- 要邀请外部客户保存真实项目；
- 需要换电脑继续工作；
- 需要分享项目；
- 需要用户权限；
- 需要备份、版本或审计；
- 项目数据丢失会造成实际损失。

推荐第一步：

- Supabase Auth；
- Supabase Postgres；
- `projects` + `project_documents`；
- 每个项目文档保存完整 `HarnessConfig` JSONB；
- RLS 强制 `owner_id = auth.uid()`；
- 乐观并发字段 `revision`/`updated_at`；
- 前端继续使用 `ProjectRepository`，替换实现即可。

暂时不需要 NestJS。只有出现复杂报价规则、跨系统集成、服务端文件生成、审批或多表事务时，再增加业务 API 层。

保存并发、乐观锁和 Realtime 的详细设计见
`SAVE_CONCURRENCY_AND_REALTIME_STRATEGY.md`。

---

## 7. 需要产品方尽快确认的问题

这些问题比“是否上后端”更影响前端 MVP 是否做对：

1. MVP 用户是内部工程师、销售、客户，还是工厂？
2. MVP 产出是概念图、询价资料，还是可生产图纸？
3. BOM 是否必须包含端子、密封件、标签、号码管和工艺？
4. 报价使用 USD 还是 CNY？是否含税、运费和 MOQ？
5. 连接器目录由谁维护和审核？
6. `PVR` 是真实材料名称还是 `PUR` 等材料的笔误？
7. 1024px 是否必须支持，还是可明确限定 1280px 以上？
8. 外部试用者的数据是否允许只保存在本机？

在这些问题没有确认前，不建议扩展更多模板、3D 效果或复杂后端。

---

## 8. 最终建议

**现在可以先把前端 MVP 做好，且这是更合适的顺序。**

但建议把决策表述为：

> 先完成“可验证、可备份、可恢复、边界诚实”的本地前端 MVP；
> 外部客户真实使用前，再接入 Supabase 作为轻量后端。

不要把“暂不做后端”理解为：

- 暂不考虑数据边界；
- 暂不做运行时校验；
- 暂不做导入导出；
- 暂不区分估算和正式报价；
- 可以把本地明文密码当认证；
- 可以把演示 BOM 当生产 BOM。

当前前端已具备继续迭代的良好基础。下一步真正值得投入的不是换技术栈，而是让用户相信：**取消不会偷偷修改、保存不会莫名丢失、错误能够恢复、输出不会被误当成正式生产资料。**
